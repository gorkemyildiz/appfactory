# Supabase

Optional client infrastructure is available in packages/database. Sprint 1 stores mock projects in browser localStorage and does not access Supabase. Migrations, authentication, and RLS must be added before enabling database persistence. Never expose service-role credentials in the web app.
