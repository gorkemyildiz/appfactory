{
  "files": [
    {
      "path": "src/features/models.ts",
      "code": "export type Coordinates = { latitude: number; longitude: number };\nexport type SpotColor = \"green\" | \"yellow\" | \"red\" | \"expired\";\nexport type Spot = {\n  id: string;\n  ownerId: string;\n  latitude: number;\n  longitude: number;\n  description: string;\n  photoPath: string;\n  photoUrl: string | null;\n  createdAtMs: number;\n  expiresAtMs: number;\n};\nexport type Wallet = {\n  dailyRights: number;\n  bonusRights: number;\n  points: number;\n  day: string;\n};\nexport type SessionUser = { userId: string; email: string | null };\nexport type PhotoDraft = { uri: string; base64: string; mimeType: string };\nexport type LocalDraft = { description: string };\n\nexport function isCoordinates(value: unknown): value is Coordinates {\n  if (!value || typeof value !== \"object\") return false;\n  const point = value as Record<string, unknown>;\n  return typeof point.latitude === \"number\" && Number.isFinite(point.latitude) && point.latitude >= -90 && point.latitude <= 90 &&\n    typeof point.longitude === \"number\" && Number.isFinite(point.longitude) && point.longitude >= -180 && point.longitude <= 180;\n}\nexport function isSpot(value: unknown): value is Spot {\n  if (!value || typeof value !== \"object\") return false;\n  const spot = value as Record<string, unknown>;\n  return typeof spot.id === \"string\" && spot.id.length > 0 && typeof spot.ownerId === \"string\" &&\n    isCoordinates(spot) && typeof spot.description === \"string\" && typeof spot.photoPath === \"string\" &&\n    (spot.photoUrl === null || typeof spot.photoUrl === \"string\") &&\n    typeof spot.createdAtMs === \"number\" && Number.isFinite(spot.createdAtMs) &&\n    typeof spot.expiresAtMs === \"number\" && Number.isFinite(spot.expiresAtMs) && spot.expiresAtMs > spot.createdAtMs;\n}\nexport function isSpotList(value: unknown): value is Spot[] {\n  return Array.isArray(value) && value.every(isSpot);\n}\nexport function isWallet(value: unknown): value is Wallet {\n  if (!value || typeof value !== \"object\") return false;\n  const wallet = value as Record<string, unknown>;\n  return Number.isInteger(wallet.dailyRights) && (wallet.dailyRights as number) >= 0 &&\n    Number.isInteger(wallet.bonusRights) && (wallet.bonusRights as number) >= 0 &&\n    Number.isInteger(wallet.points) && (wallet.points as number) >= 0 &&\n    typeof wallet.day === \"string\" && /^\\d{4}-\\d{2}-\\d{2}$/.test(wallet.day);\n}\nexport function isLocalDraft(value: unknown): value is LocalDraft {\n  return !!value && typeof value === \"object\" && typeof (value as Record<string, unknown>).description === \"string\";\n}\nexport function isSessionUser(value: unknown): value is SessionUser {\n  return !!value && typeof value === \"object\" && typeof (value as Record<string, unknown>).userId === \"string\" &&\n    (((value as Record<string, unknown>).email === null) || typeof (value as Record<string, unknown>).email === \"string\");\n}\n"
    },
    {
      "path": "src/features/domain.ts",
      "code": "import type { Coordinates, Spot, SpotColor, Wallet } from \"./models\";\n\nexport const SPOT_LIFETIME_MS = 7 * 60_000;\nexport const SEARCH_RADIUS_METERS = 100;\nexport const POINTS_PER_SUCCESSFUL_USE = 1;\n\nexport function spotColor(createdAtMs: number, nowMs: number): SpotColor {\n  const elapsed = nowMs - createdAtMs;\n  if (elapsed >= SPOT_LIFETIME_MS) return \"expired\";\n  if (elapsed >= 5 * 60_000) return \"red\";\n  if (elapsed >= 3 * 60_000) return \"yellow\";\n  return \"green\";\n}\nexport function remainingSpotMs(createdAtMs: number, nowMs: number): number {\n  return Math.max(0, SPOT_LIFETIME_MS - Math.max(0, nowMs - createdAtMs));\n}\nfunction distanceMeters(a: Coordinates, b: Coordinates): number {\n  const radians = (degrees: number) => degrees * Math.PI / 180;\n  const lat1 = radians(a.latitude);\n  const lat2 = radians(b.latitude);\n  const dLat = lat2 - lat1;\n  const dLon = radians(b.longitude - a.longitude);\n  const h = Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLon / 2) ** 2;\n  return 6_371_000 * 2 * Math.atan2(Math.sqrt(h), Math.sqrt(1 - h));\n}\nexport function nearbyActiveSpots(spots: Spot[], center: Coordinates, nowMs: number, radiusMeters = SEARCH_RADIUS_METERS): Spot[] {\n  return spots.filter((spot) => spot.expiresAtMs > nowMs && distanceMeters(center, spot) <= radiusMeters);\n}\nexport function resetDailyRights(wallet: Wallet, utcDay: string): Wallet {\n  return wallet.day === utcDay ? wallet : { ...wallet, dailyRights: 1, day: utcDay };\n}\nexport function canUseSpot(spot: Spot, userId: string, nowMs: number): boolean {\n  return spot.ownerId !== userId && spot.expiresAtMs > nowMs;\n}\n\n// JSON rule checks (arguments are serialized as an array).\nexport const ruleTests = [\n  { name: \"Renk eşikleri tam dakikalarda değişir\", exportName: \"spotColor\", argsJson: \"[0,180000]\", expectedJson: \"\\\"yellow\\\"\" },\n  { name: \"Beşinci dakikada kırmızı, yedinci dakikada sona erer\", exportName: \"spotColor\", argsJson: \"[0,300000]\", expectedJson: \"\\\"red\\\"\" },\n  { name: \"Bitiş anında kalan süre sıfırdır\", exportName: \"remainingSpotMs\", argsJson: \"[1000,421000]\", expectedJson: \"0\" },\n  { name: \"Yakın arama bitiş anındaki kaydı dışlar\", exportName: \"nearbyActiveSpots\", argsJson: \"[[{\\\"id\\\":\\\"a\\\",\\\"ownerId\\\":\\\"u\\\",\\\"latitude\\\":0,\\\"longitude\\\":0,\\\"description\\\":\\\"\\\",\\\"photoPath\\\":\\\"u/a.jpg\\\",\\\"photoUrl\\\":null,\\\"createdAtMs\\\":0,\\\"expiresAtMs\\\":420000}],{\\\"latitude\\\":0,\\\"longitude\\\":0},420000]\", expectedJson: \"[]\" },\n  { name: \"Gün değişince günlük hak bir olur ve bonus korunur\", exportName: \"resetDailyRights\", argsJson: \"[{\\\"dailyRights\\\":0,\\\"bonusRights\\\":2,\\\"points\\\":3,\\\"day\\\":\\\"2025-01-01\\\"},\\\"2025-01-02\\\"]\", expectedJson: \"{\\\"dailyRights\\\":1,\\\"bonusRights\\\":2,\\\"points\\\":3,\\\"day\\\":\\\"2025-01-02\\\"}\" }\n] as const;\n"
    },
    {
      "path": "src/features/services.ts",
      "code": "import { backendConfigured, capturePhoto as runtimeCapturePhoto, currentLocation, getBackend, photoUrl, readLocal, uploadPhoto, writeLocal } from \"../runtime/runtime\";\nimport type { CapturedPhoto } from \"../runtime/runtime\";\nimport { isLocalDraft, isSpot, isWallet } from \"./models\";\nimport type { Coordinates, LocalDraft, PhotoDraft, SessionUser, Spot, Wallet } from \"./models\";\n\nconst PHOTO_BUCKET = \"park-spot-photos\";\nconst DRAFT_KEY = \"parkspot-draft-v1\";\nfunction requireBackend() {\n  if (!backendConfigured) throw new Error(\"Paylaşılan park yeri özellikleri için Supabase bağlantısı gerekli.\");\n  return getBackend();\n}\nfunction check(error: { message: string } | null, fallback: string): void {\n  if (error) throw new Error(error.message || fallback);\n}\nfunction sessionUser(user: { id: string; email?: string | null } | null): SessionUser | null {\n  return user ? { userId: user.id, email: user.email ?? null } : null;\n}\nexport async function getSession(): Promise<SessionUser | null> {\n  if (!backendConfigured) return null;\n  const { data, error } = await requireBackend().auth.getSession();\n  check(error, \"Oturum alınamadı.\");\n  return sessionUser(data.session?.user ?? null);\n}\nexport function subscribeAuth(callback: (user: SessionUser | null) => void): () => void {\n  const { data } = requireBackend().auth.onAuthStateChange((_event, session) => callback(sessionUser(session?.user ?? null)));\n  return () => data.subscription.unsubscribe();\n}\nexport async function signUp(email: string, password: string, fullName: string): Promise<void> {\n  const { error } = await requireBackend().auth.signUp({ email: email.trim(), password, options: { data: { full_name: fullName.trim() } } });\n  check(error, \"Kayıt başarısız.\");\n}\nexport async function signIn(email: string, password: string): Promise<void> {\n  const { error } = await requireBackend().auth.signInWithPassword({ email: email.trim(), password });\n  check(error, \"Giriş başarısız.\");\n}\nexport async function signOut(): Promise<void> {\n  const { error } = await requireBackend().auth.signOut();\n  check(error, \"Oturum kapatılamadı.\");\n}\nexport async function loadLocalDraft(): Promise<LocalDraft> {\n  return readLocal(DRAFT_KEY, isLocalDraft, { description: \"\" });\n}\nexport async function saveLocalDraft(draft: LocalDraft): Promise<void> {\n  await writeLocal(DRAFT_KEY, draft);\n}\nexport async function getCurrentLocation(): Promise<Coordinates> {\n  return currentLocation();\n}\nexport async function takePhoto(): Promise<PhotoDraft | null> {\n  const photo = await runtimeCapturePhoto();\n  return photo ? { uri: photo.uri, base64: photo.base64, mimeType: photo.mimeType } : null;\n}\nfunction mapSpot(raw: Record<string, unknown>): Spot {\n  const spot: Spot = {\n    id: String(raw.id), ownerId: String(raw.owner_id), latitude: Number(raw.latitude), longitude: Number(raw.longitude),\n    description: String(raw.description ?? \"\"), photoPath: String(raw.photo_path), photoUrl: null,\n    createdAtMs: Date.parse(String(raw.created_at)), expiresAtMs: Date.parse(String(raw.expires_at)),\n  };\n  if (!isSpot(spot)) throw new Error(\"Sunucudan geçersiz park yeri verisi alındı.\");\n  return spot;\n}\nexport async function nearbySpots(center: Coordinates): Promise<Spot[]> {\n  const { data, error } = await requireBackend().rpc(\"nearby_spots\", { p_latitude: center.latitude, p_longitude: center.longitude });\n  check(error, \"Yakındaki park yerleri alınamadı.\");\n  if (!Array.isArray(data)) throw new Error(\"Park yeri yanıtı geçersiz.\");\n  const spots = data.map((row) => mapSpot(row as Record<string, unknown>));\n  return Promise.all(spots.map(async (spot) => {\n    try { return { ...spot, photoUrl: await photoUrl(PHOTO_BUCKET, spot.photoPath) }; }\n    catch { return spot; }\n  }));\n}\nexport async function getWallet(): Promise<Wallet> {\n  const { data, error } = await requireBackend().rpc(\"get_my_wallet\");\n  check(error, \"Kullanım hakları alınamadı.\");\n  const raw = data as Record<string, unknown> | null;\n  const wallet = raw ? { dailyRights: Number(raw.dailyRights), bonusRights: Number(raw.bonusRights), points: Number(raw.points), day: String(raw.day) } : null;\n  if (!isWallet(wallet)) throw new Error(\"Sunucudan geçersiz hak bilgisi alındı.\");\n  return wallet;\n}\nexport async function publishSpot(center: Coordinates, description: string, photo: PhotoDraft): Promise<void> {\n  const client = requireBackend();\n  const { data: auth, error: authError } = await client.auth.getUser();\n  check(authError, \"Oturum doğrulanamadı.\");\n  if (!auth.user) throw new Error(\"Park yeri paylaşmak için giriş yapın.\");\n  const uploaded = await uploadPhoto(PHOTO_BUCKET, photo as CapturedPhoto);\n  const { error } = await client.rpc(\"create_park_spot\", {\n    p_latitude: center.latitude, p_longitude: center.longitude, p_description: description.trim(), p_photo_path: uploaded,\n  });\n  check(error, \"Park yeri paylaşılamadı.\");\n}\nexport async function reportOccupied(spotId: string): Promise<boolean> {\n  const { data, error } = await requireBackend().rpc(\"report_occupied\", { p_spot_id: spotId });\n  check(error, \"Dolu bildirimi gönderilemedi.\");\n  if (typeof data !== \"boolean\") throw new Error(\"Bildirim yanıtı geçersiz.\");\n  return data;\n}\nexport async function claimSuccessfulUse(spotId: string): Promise<Wallet> {\n  const { error } = await requireBackend().rpc(\"claim_successful_use\", { p_spot_id: spotId });\n  check(error, \"Kullanım doğrulanamadı.\");\n  return getWallet();\n}\nexport async function grantPrototypeVideoRight(): Promise<Wallet> {\n  const { error } = await requireBackend().rpc(\"grant_video_prototype_right\");\n  check(error, \"Prototip hakkı eklenemedi.\");\n  return getWallet();\n}\nexport async function redeemPoint(): Promise<Wallet> {\n  const { error } = await requireBackend().rpc(\"redeem_point_for_right\");\n  check(error, \"Puan kullanılamadı.\");\n  return getWallet();\n}\n"
    },
    {
      "path": "src/features/store.tsx",
      "code": "import { createContext, useContext, useEffect, useRef, useState } from \"react\";\nimport type { ReactNode } from \"react\";\nimport { backendConfigured } from \"../runtime/runtime\";\nimport { canUseSpot, nearbyActiveSpots } from \"./domain\";\nimport type { Coordinates, LocalDraft, PhotoDraft, SessionUser, Spot, Wallet } from \"./models\";\nimport { getCurrentLocation, getSession, getWallet, grantPrototypeVideoRight, nearbySpots, publishSpot, redeemPoint, reportOccupied, saveLocalDraft, signIn as serviceSignIn, signOut as serviceSignOut, signUp as serviceSignUp, subscribeAuth, takePhoto, claimSuccessfulUse, loadLocalDraft } from \"./services\";\n\ntype AppContextValue = {\n  loading: boolean; busy: boolean; busyAction: string | null; error: string | null; setupRequired: boolean;\n  session: SessionUser | null; location: Coordinates | null; spots: Spot[]; wallet: Wallet | null;\n  draft: LocalDraft; photoDraft: PhotoDraft | null; selectedSpotId: string | null;\n  clearError: () => void; updateLocation: () => Promise<void>; refresh: () => Promise<void>;\n  selectSpot: (id: string | null) => void; captureSpotPhoto: () => Promise<void>;\n  updateDescription: (description: string) => Promise<void>;\n  createSpot: (description: string, photo?: PhotoDraft | null) => Promise<void>;\n  reportSpotOccupied: (id: string) => Promise<void>; confirmSuccessfulUse: (id: string) => Promise<void>;\n  watchVideoPrototype: () => Promise<void>; redeemPointForRight: () => Promise<void>;\n  signUp: (email: string, password: string, fullName: string) => Promise<void>;\n  signIn: (email: string, password: string) => Promise<void>; signOut: () => Promise<void>;\n};\nconst AppContext = createContext<AppContextValue | null>(null);\nexport function AppProvider({ children }: { children: ReactNode }) {\n  const [loading, setLoading] = useState(true);\n  const [busy, setBusy] = useState(false);\n  const [busyAction, setBusyAction] = useState<string | null>(null);\n  const [error, setError] = useState<string | null>(null);\n  const [setupRequired, setSetupRequired] = useState(!backendConfigured);\n  const [session, setSession] = useState<SessionUser | null>(null);\n  const [location, setLocation] = useState<Coordinates | null>(null);\n  const [spots, setSpots] = useState<Spot[]>([]);\n  const [wallet, setWallet] = useState<Wallet | null>(null);\n  const [draft, setDraft] = useState<LocalDraft>({ description: \"\" });\n  const [photoDraft, setPhotoDraft] = useState<PhotoDraft | null>(null);\n  const [selectedSpotId, selectSpot] = useState<string | null>(null);\n  const queue = useRef<Promise<void>>(Promise.resolve());\n  const busyKeys = useRef(new Set<string>());\n  const refreshRef = useRef<() => Promise<void>>(async () => undefined);\n\n  const refresh = async () => {\n    if (!backendConfigured || !session) return;\n    try {\n      const [nextWallet, nextSpots] = await Promise.all([\n        getWallet(), location ? nearbySpots(location) : Promise.resolve(spots),\n      ]);\n      setWallet(nextWallet);\n      setSpots(nextSpots);\n    } catch (cause) { setError(cause instanceof Error ? cause.message : \"Veriler yenilenemedi.\"); }\n  };\n  refreshRef.current = refresh;\n  useEffect(() => {\n    let alive = true;\n    let unsubscribe: (() => void) | undefined;\n    const start = async () => {\n      try {\n        const savedDraft = await loadLocalDraft();\n        if (alive) setDraft(savedDraft);\n        if (!backendConfigured) return;\n        const user = await getSession();\n        if (alive) { setSession(user); setSetupRequired(false); }\n        if (user) {\n          const nextWallet = await getWallet();\n          if (alive) setWallet(nextWallet);\n        }\n        unsubscribe = subscribeAuth((next) => {\n          if (!alive) return;\n          setSession(next);\n          setWallet(null);\n          setSpots([]);\n          if (!next) return;\n          void getWallet().then((value) => { if (alive) setWallet(value); }).catch((cause) => { if (alive) setError(cause instanceof Error ? cause.message : \"Haklar alınamadı.\"); });\n        });\n      } catch (cause) {\n        if (alive) {\n          setError(cause instanceof Error ? cause.message : \"Başlatma başarısız.\");\n          if (backendConfigured) setSetupRequired(true);\n        }\n      } finally { if (alive) setLoading(false); }\n    };\n    void start();\n    return () => { alive = false; unsubscribe?.(); };\n  }, []);\n  useEffect(() => {\n    if (!session || !location) return;\n    const timer = setInterval(() => { void refreshRef.current(); }, 15_000);\n    return () => clearInterval(timer);\n  }, [session, location]);\n\n  const runWrite = async (key: string, action: () => Promise<void>) => {\n    if (busyKeys.current.has(key)) return;\n    busyKeys.current.add(key);\n    setBusy(true); setBusyAction(key); setError(null);\n    const operation = queue.current.then(action);\n    queue.current = operation.catch(() => undefined);\n    try { await operation; }\n    catch (cause) { setError(cause instanceof Error ? cause.message : \"İşlem başarısız.\"); }\n    finally { busyKeys.current.delete(key); setBusy(busyKeys.current.size > 0); setBusyAction(busyKeys.current.values().next().value ?? null); }\n  };\n  const updateLocation = async () => {\n    try {\n      const point = await getCurrentLocation();\n      setLocation(point);\n      if (session) {\n        const next = await nearbySpots(point);\n        setSpots(next);\n      }\n    } catch (cause) { setError(cause instanceof Error ? cause.message : \"Konum alınamadı.\"); }\n  };\n  const captureSpotPhoto = async () => {\n    try { setPhotoDraft(await takePhoto()); }\n    catch (cause) { setError(cause instanceof Error ? cause.message : \"Fotoğraf çekilemedi.\"); }\n  };\n  const updateDescription = (description: string) => runWrite(\"draft\", async () => {\n    const next = { description };\n    await saveLocalDraft(next);\n    setDraft(next);\n  });\n  const createSpot = (description: string, photo = photoDraft) => runWrite(\"createSpot\", async () => {\n    if (!session) throw new Error(backendConfigured ? \"Önce giriş yapın.\" : \"Paylaşım için Supabase kurulumu gerekli.\");\n    if (!location) throw new Error(\"Önce konumunuzu güncelleyin.\");\n    if (!photo) throw new Error(\"Park yerini doğrulamak için fotoğraf çekin.\");\n    if (!description.trim()) throw new Error(\"Kısa bir açıklama yazın.\");\n    await publishSpot(location, description, photo);\n    setPhotoDraft(null);\n    const fresh = await nearbySpots(location);\n    setSpots(fresh);\n  });\n  const reportSpotOccupied = (id: string) => runWrite(`report:${id}`, async () => {\n    await reportOccupied(id);\n    if (location) setSpots(await nearbySpots(location));\n  });\n  const confirmSuccessfulUse = (id: string) => runWrite(`claim:${id}`, async () => {\n    const spot = spots.find((item) => item.id === id);\n    if (!session || !spot || !canUseSpot(spot, session.userId, Date.now())) throw new Error(\"Bu park yeri kullanılamıyor.\");\n    const nextWallet = await claimSuccessfulUse(id);\n    setWallet(nextWallet);\n    if (location) setSpots(await nearbySpots(location));\n  });\n  const watchVideoPrototype = () => runWrite(\"videoPrototype\", async () => {\n    if (!session) throw new Error(\"Ek hak almak için giriş yapın.\");\n    setWallet(await grantPrototypeVideoRight());\n  });\n  const redeemPointForRight = () => runWrite(\"redeemPoint\", async () => {\n    if (!session) throw new Error(\"Puan kullanmak için giriş yapın.\");\n    setWallet(await redeemPoint());\n  });\n  const signUp = (email: string, password: string, fullName: string) => runWrite(\"signUp\", async () => {\n    if (!email.trim() || password.length < 8 || !fullName.trim()) throw new Error(\"Ad soyad, e-posta ve en az 8 karakterli şifre girin.\");\n    await serviceSignUp(email, password, fullName);\n  });\n  const signIn = (email: string, password: string) => runWrite(\"signIn\", async () => {\n    await serviceSignIn(email, password);\n  });\n  const signOut = () => runWrite(\"signOut\", async () => { await serviceSignOut(); });\n  const value: AppContextValue = {\n    loading, busy, busyAction, error, setupRequired, session, location,\n    spots: location ? nearbyActiveSpots(spots, location, Date.now()) : spots, wallet, draft, photoDraft,\n    selectedSpotId, clearError: () => setError(null), updateLocation, refresh, selectSpot, captureSpotPhoto,\n    updateDescription, createSpot, reportSpotOccupied, confirmSuccessfulUse, watchVideoPrototype,\n    redeemPointForRight, signUp, signIn, signOut,\n  };\n  return <AppContext.Provider value={value}>{children}</AppContext.Provider>;\n}\nexport function useApp(): AppContextValue {\n  const value = useContext(AppContext);\n  if (!value) throw new Error(\"useApp, AppProvider içinde kullanılmalıdır.\");\n  return value;\n}\n"
    }
  ],
  "summary": "ParkSpot için ortak iş kurallarını, Expo uygulamalarına bağlanabilir durum sağlayıcısını ve Supabase servis katmanını hazırladım. Park yeri işaretleri sunucu tarafında yedi dakika geçerli tutulur; renk eşikleri, 100 metre yarıçapındaki arama, fotoğraf doğrulaması, dolu bildirimleri, kullanım hakları ve başarılı kullanım puanı uygulanır. Harici reklam ağı entegrasyonu yerine, sunucu tarafında günde bir kez sınırlandırılan video-ödül prototipi sağlanır.",
  "limitations": [
    "Gerçek reklam/video doğrulaması kapsam dışıdır; prototip RPC'si günde en fazla bir ek hak verir ve reklam izlenmiş olduğunu kanıtlamaz.",
    "Kayıt e-posta ve parola içindir; telefonla kayıt bu kapsamda uygulanmadı.",
    "Supabase bağlantısı ve migration kurulmadan çok kullanıcılı paylaşım, haklar ve puan işlemleri kullanılamaz. Yerel saklama bunların yerine geçmez.",
    "Park yeri fotoğrafı kamera ile çekilir; galeri seçimi bu özellik kapsamında sunulmamıştır.",
    "Konum ve kamera izinleri yalnızca ilgili kullanıcı eyleminde istenir. Harita sunumu, sağlanan runtime/map.tsx ekran bileşenini kullanır."
  ],
  "capabilities": [
    "storage",
    "backend",
    "location",
    "camera",
    "maps"
  ],
  "migrationSql": "create table if not exists public.park_spots (\n  id uuid primary key default gen_random_uuid(),\n  owner_id uuid not null references auth.users(id) on delete cascade,\n  latitude double precision not null check (latitude between -90 and 90),\n  longitude double precision not null check (longitude between -180 and 180),\n  description text not null default '' check (char_length(description) <= 500),\n  photo_path text not null,\n  created_at timestamptz not null default now(),\n  expires_at timestamptz not null,\n  constraint park_spots_seven_minute_lifetime check (expires_at = created_at + interval '7 minutes')\n);\ncreate index if not exists park_spots_expiry_idx on public.park_spots (expires_at);\ncreate index if not exists park_spots_owner_idx on public.park_spots (owner_id);\ncreate table if not exists public.park_wallets (\n  user_id uuid primary key references auth.users(id) on delete cascade,\n  daily_day date not null,\n  daily_rights integer not null default 1 check (daily_rights >= 0),\n  bonus_rights integer not null default 0 check (bonus_rights >= 0),\n  points integer not null default 0 check (points >= 0),\n  updated_at timestamptz not null default now()\n);\ncreate table if not exists public.park_occupied_reports (\n  spot_id uuid not null references public.park_spots(id) on delete cascade,\n  reporter_id uuid not null references auth.users(id) on delete cascade,\n  created_at timestamptz not null default now(),\n  primary key (spot_id, reporter_id)\n);\ncreate index if not exists park_reports_spot_idx on public.park_occupied_reports (spot_id);\ncreate table if not exists public.park_successful_claims (\n  spot_id uuid primary key,\n  claimant_id uuid not null references auth.users(id) on delete cascade,\n  owner_id uuid not null references auth.users(id) on delete cascade,\n  created_at timestamptz not null default now(),\n  constraint park_claim_not_self check (claimant_id <> owner_id)\n);\ncreate index if not exists park_claims_claimant_idx on public.park_successful_claims (claimant_id, created_at);\ncreate table if not exists public.park_video_prototype_grants (\n  user_id uuid not null references auth.users(id) on delete cascade,\n  grant_day date not null,\n  created_at timestamptz not null default now(),\n  primary key (user_id, grant_day)\n);\n\nalter table public.park_spots enable row level security;\nalter table public.park_wallets enable row level security;\nalter table public.park_occupied_reports enable row level security;\nalter table public.park_successful_claims enable row level security;\nalter table public.park_video_prototype_grants enable row level security;\ndrop policy if exists park_spots_read_active on public.park_spots;\ncreate policy park_spots_read_active on public.park_spots for select to authenticated using (expires_at > now());\ndrop policy if exists park_wallets_read_own on public.park_wallets;\ncreate policy park_wallets_read_own on public.park_wallets for select to authenticated using (user_id = (select auth.uid()));\n\ninsert into storage.buckets (id, name, public) values ('park-spot-photos', 'park-spot-photos', false) on conflict (id) do update set public = false;\ndrop policy if exists park_photos_authenticated_read on storage.objects;\ncreate policy park_photos_authenticated_read on storage.objects for select to authenticated using (bucket_id = 'park-spot-photos');\ndrop policy if exists park_photos_owner_insert on storage.objects;\ncreate policy park_photos_owner_insert on storage.objects for insert to authenticated with check (bucket_id = 'park-spot-photos' and (storage.foldername(name))[1] = (select auth.uid())::text);\ndrop policy if exists park_photos_owner_delete on storage.objects;\ncreate policy park_photos_owner_delete on storage.objects for delete to authenticated using (bucket_id = 'park-spot-photos' and (storage.foldername(name))[1] = (select auth.uid())::text);\n\ncreate or replace function public._ensure_park_wallet(p_user uuid) returns void\nlanguage plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_day date := (now() at time zone 'UTC')::date;\nbegin\n  if auth.uid() is null then raise exception 'Giriş gerekli'; end if;\n  insert into public.park_wallets(user_id, daily_day, daily_rights) values (p_user, v_day, 1)\n  on conflict (user_id) do update set daily_rights = case when park_wallets.daily_day <> v_day then 1 else park_wallets.daily_rights end,\n    daily_day = v_day, updated_at = case when park_wallets.daily_day <> v_day then now() else park_wallets.updated_at end;\nend; $$;\nrevoke all on function public._ensure_park_wallet(uuid) from public, anon, authenticated;\n\ncreate or replace function public.get_my_wallet() returns jsonb\nlanguage plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_uid uuid := auth.uid(); v_row public.park_wallets%rowtype;\nbegin\n  if v_uid is null then raise exception 'Giriş gerekli'; end if;\n  perform public._ensure_park_wallet(v_uid);\n  select * into v_row from public.park_wallets where user_id = v_uid;\n  return jsonb_build_object('dailyRights', v_row.daily_rights, 'bonusRights', v_row.bonus_rights, 'points', v_row.points, 'day', v_row.daily_day::text);\nend; $$;\n\ncreate or replace function public.nearby_spots(p_latitude double precision, p_longitude double precision)\nreturns setof public.park_spots language plpgsql security invoker set search_path = public, pg_temp as $$\nbegin\n  if auth.uid() is null then raise exception 'Giriş gerekli'; end if;\n  if p_latitude is null or p_longitude is null or p_latitude not between -90 and 90 or p_longitude not between -180 and 180 then raise exception 'Konum geçersiz'; end if;\n  return query select s.* from public.park_spots s\n  where s.expires_at > now()\n    and 6371000 * 2 * asin(sqrt(least(1.0, greatest(0.0,\n      sin(radians(s.latitude - p_latitude) / 2)^2 + cos(radians(p_latitude)) * cos(radians(s.latitude)) * sin(radians(s.longitude - p_longitude) / 2)^2\n    )))) <= 100;\nend; $$;\n\ncreate or replace function public.create_park_spot(p_latitude double precision, p_longitude double precision, p_description text, p_photo_path text)\nreturns uuid language plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_uid uuid := auth.uid(); v_id uuid;\nbegin\n  if v_uid is null then raise exception 'Giriş gerekli'; end if;\n  if p_latitude is null or p_longitude is null or p_latitude not between -90 and 90 or p_longitude not between -180 and 180 then raise exception 'Konum geçersiz'; end if;\n  if p_description is null or char_length(trim(p_description)) = 0 or char_length(p_description) > 500 then raise exception 'Açıklama geçersiz'; end if;\n  if p_photo_path is null or (storage.foldername(p_photo_path))[1] <> v_uid::text then raise exception 'Fotoğraf yolu kullanıcıyla eşleşmiyor'; end if;\n  insert into public.park_spots(owner_id, latitude, longitude, description, photo_path, created_at, expires_at)\n  values (v_uid, p_latitude, p_longitude, trim(p_description), p_photo_path, now(), now() + interval '7 minutes') returning id into v_id;\n  return v_id;\nend; $$;\n\ncreate or replace function public.report_occupied(p_spot_id uuid) returns boolean\nlanguage plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_uid uuid := auth.uid(); v_owner uuid; v_count integer;\nbegin\n  if v_uid is null then raise exception 'Giriş gerekli'; end if;\n  select owner_id into v_owner from public.park_spots where id = p_spot_id and expires_at > now() for update;\n  if not found then raise exception 'Park yeri bulunamadı veya süresi doldu'; end if;\n  if v_owner = v_uid then raise exception 'Kendi işaretinize dolu bildirimi gönderemezsiniz'; end if;\n  insert into public.park_occupied_reports(spot_id, reporter_id) values (p_spot_id, v_uid) on conflict do nothing;\n  select count(*) into v_count from public.park_occupied_reports where spot_id = p_spot_id;\n  if v_count >= 2 then delete from public.park_spots where id = p_spot_id; return true; end if;\n  return false;\nend; $$;\n\ncreate or replace function public.claim_successful_use(p_spot_id uuid) returns void\nlanguage plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_uid uuid := auth.uid(); v_owner uuid; v_wallet public.park_wallets%rowtype;\nbegin\n  if v_uid is null then raise exception 'Giriş gerekli'; end if;\n  select owner_id into v_owner from public.park_spots where id = p_spot_id and expires_at > now() for update;\n  if not found then raise exception 'Park yeri bulunamadı veya süresi doldu'; end if;\n  if v_owner = v_uid then raise exception 'Kendi işaretinizi kullanamazsınız'; end if;\n  perform public._ensure_park_wallet(v_uid);\n  select * into v_wallet from public.park_wallets where user_id = v_uid for update;\n  if v_wallet.daily_rights > 0 then\n    update public.park_wallets set daily_rights = daily_rights - 1, updated_at = now() where user_id = v_uid;\n  elsif v_wallet.bonus_rights > 0 then\n    update public.park_wallets set bonus_rights = bonus_rights - 1, updated_at = now() where user_id = v_uid;\n  elsif v_wallet.points > 0 then\n    update public.park_wallets set points = points - 1, updated_at = now() where user_id = v_uid;\n  else raise exception 'Kullanım hakkınız veya puanınız yok'; end if;\n  perform public._ensure_park_wallet(v_owner);\n  insert into public.park_successful_claims(spot_id, claimant_id, owner_id) values (p_spot_id, v_uid, v_owner);\n  update public.park_wallets set points = points + 1, updated_at = now() where user_id = v_owner;\n  delete from public.park_spots where id = p_spot_id;\nend; $$;\n\ncreate or replace function public.grant_video_prototype_right() returns void\nlanguage plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_uid uuid := auth.uid(); v_day date := (now() at time zone 'UTC')::date;\nbegin\n  if v_uid is null then raise exception 'Giriş gerekli'; end if;\n  perform public._ensure_park_wallet(v_uid);\n  insert into public.park_video_prototype_grants(user_id, grant_day) values (v_uid, v_day);\n  update public.park_wallets set bonus_rights = bonus_rights + 1, updated_at = now() where user_id = v_uid;\nend; $$;\n\ncreate or replace function public.redeem_point_for_right() returns void\nlanguage plpgsql security definer set search_path = public, pg_temp as $$\ndeclare v_uid uuid := auth.uid();\nbegin\n  if v_uid is null then raise exception 'Giriş gerekli'; end if;\n  perform public._ensure_park_wallet(v_uid);\n  update public.park_wallets set points = points - 1, bonus_rights = bonus_rights + 1, updated_at = now()\n  where user_id = v_uid and points > 0;\n  if not found then raise exception 'Yeterli puan yok'; end if;\nend; $$;\n\nrevoke all on function public.get_my_wallet() from public, anon;\ngrant execute on function public.get_my_wallet() to authenticated;\nrevoke all on function public.nearby_spots(double precision, double precision) from public, anon;\ngrant execute on function public.nearby_spots(double precision, double precision) to authenticated;\nrevoke all on function public.create_park_spot(double precision, double precision, text, text) from public, anon;\ngrant execute on function public.create_park_spot(double precision, double precision, text, text) to authenticated;\nrevoke all on function public.report_occupied(uuid) from public, anon;\ngrant execute on function public.report_occupied(uuid) to authenticated;\nrevoke all on function public.claim_successful_use(uuid) from public, anon;\ngrant execute on function public.claim_successful_use(uuid) to authenticated;\nrevoke all on function public.grant_video_prototype_right() from public, anon;\ngrant execute on function public.grant_video_prototype_right() to authenticated;\nrevoke all on function public.redeem_point_for_right() from public, anon;\ngrant execute on function public.redeem_point_for_right() to authenticated;\n",
  "setup": [
    "Supabase projesi oluşturun ve src/runtime/connection.json içine HTTPS proje URL'si ile sb_publishable_ anahtarını girin; gizli/service_role anahtarını istemciye koymayın.",
    "migrationSql içeriğini inceleyip Supabase SQL Editor veya migration sistemiyle uygulayın. SQL otomatik olarak dağıtılmaz; dağıtılana kadar backend özellikleri kurulum bekler.",
    "Supabase Auth e-posta/parola sağlayıcısını ve e-posta doğrulama ayarlarını yapılandırın. Uygulama kayıt sırasında ad soyadı user metadata alanında saklar.",
    "Expo hedeflerinde kamera, konum ve react-native-maps için gereken platform izinlerini/yerel yapılandırmayı ayarlayın. Kullanıcı izinleri yalnızca konum güncelleme veya fotoğraf çekme eyleminde istenir.",
    "RLS, RPC erişimleri ve özel park-spot-photos bucket politikalarını test edin; storage erişimi oturum açmış kullanıcılarla sınırlıdır. Harici reklam servisi veya gerçek video doğrulama kurulumu bu prototipe dahil değildir."
  ],
  "coverage": [
    {
      "requirement": "Fotoğraf doğrulaması ile yedi dakika süren park yeri işareti oluşturma",
      "status": "implemented",
      "detail": "Kamera çekimi, özel bucket'a yükleme ve sunucu saatini kullanan create_park_spot RPC'si bulunur. Süre uygulama istemcisinden kabul edilmez."
    },
    {
      "requirement": "İlk 3 dakika yeşil, 3-5 dakika sarı, 5-7 dakika kırmızı; 7 dakika sonunda kaldırma",
      "status": "implemented",
      "detail": "domain.ts eşik kurallarını içerir; RPC yakın aramasında süresi geçmiş kayıtları döndürmez. Ekran renk eşlemesi spotColor sonucunu kullanabilir."
    },
    {
      "requirement": "Kullanıcının konumunu alma ve 100 metre yarıçapındaki işaretleri haritada gösterme",
      "status": "implemented",
      "detail": "Konum açık kullanıcı eylemiyle alınır; nearby_spots sunucu RPC'si ve domain filtrelemesi 100 metre yarıçapını uygular. Harita çizimi sağlanan runtime/map.tsx bileşenine bırakılmıştır."
    },
    {
      "requirement": "Park yeri detayında fotoğraf/açıklama gösterme ve dolu bildirme; teyit edilince işareti kaldırma",
      "status": "implemented",
      "detail": "Fotoğraf için kısa süreli imzalı URL, açıklama ve report_occupied RPC desteği vardır. İki farklı kullanıcı bildirimi satır kilidi ve benzersiz rapor kısıtıyla teyit sağlar."
    },
    {
      "requirement": "Başarılı kullanımda işaretleyene puan verilmesi ve park etme hakkı kullanımı",
      "status": "implemented",
      "detail": "claim_successful_use tek transaction içinde hak/puan tüketir, kullanıcıların kendi işaretini talep etmesini engeller, işaret sahibine bir puan ekler ve işareti kaldırır. Benzersiz claim kaydı tekrar ödülü engeller."
    },
    {
      "requirement": "Her gün bir ücretsiz hak ve günlük sıfırlama",
      "status": "implemented",
      "detail": "UTC gün değişimi sunucu RPC'sinde hesaplanır; günlük hak bir olarak yenilenir. Kullanım sırası günlük hak, bonus hak ve son olarak puandır."
    },
    {
      "requirement": "Video izleme ekranı/sonrası hak kazanma prototipi",
      "status": "implemented",
      "detail": "watchVideoPrototype RPC'si kullanıcı başına UTC gününde tek ek hak verir. Gerçek reklam gösterimi ve izlenme doğrulaması kapsam dışıdır."
    },
    {
      "requirement": "Kayıt, giriş, çıkış ve oturum yaşam döngüsü",
      "status": "implemented",
      "detail": "Supabase e-posta/parola kayıt-giriş-çıkış ve auth state listener uygulanır; abonelik sağlayıcı kapanırken temizlenir. Telefon numarası kaydı yoktur."
    },
    {
      "requirement": "Ayarlar için puan, günlük hak ve video prototipi durumunu yönetme",
      "status": "implemented",
      "detail": "Store wallet alanını ve puan karşılığı bir hak ekleme aksiyonunu sunar; ayar ekranı bu state'i görüntüleyebilir."
    },
    {
      "requirement": "Backend olmadan yerel ürün işlevlerinin çalışması; ortak veriyi yerel depolama ile taklit etmeme",
      "status": "implemented",
      "detail": "Açıklama taslağı yerel saklanır; konum ve fotoğraf aksiyonları cihazda çalışır. Paylaşılan işaretler, oturum, puan ve haklar backend kurulmadan açıkça setup-required kalır."
    }
  ],
  "tests": [
    {
      "name": "Renk eşikleri tam dakikalarda değişir",
      "exportName": "spotColor",
      "argsJson": "[0,180000]",
      "expectedJson": "\"yellow\""
    },
    {
      "name": "Beşinci dakikada kırmızı",
      "exportName": "spotColor",
      "argsJson": "[0,300000]",
      "expectedJson": "\"red\""
    },
    {
      "name": "Bitiş anında kalan süre sıfırdır",
      "exportName": "remainingSpotMs",
      "argsJson": "[1000,421000]",
      "expectedJson": "0"
    },
    {
      "name": "Arama, süresi dolduğu andaki kaydı dışlar",
      "exportName": "nearbyActiveSpots",
      "argsJson": "[[{\"id\":\"a\",\"ownerId\":\"u\",\"latitude\":0,\"longitude\":0,\"description\":\"\",\"photoPath\":\"u/a.jpg\",\"photoUrl\":null,\"createdAtMs\":0,\"expiresAtMs\":420000}],{\"latitude\":0,\"longitude\":0},420000]",
      "expectedJson": "[]"
    },
    {
      "name": "Gün değişimi bir ücretsiz hak verir ve bonusları korur",
      "exportName": "resetDailyRights",
      "argsJson": "[{\"dailyRights\":0,\"bonusRights\":2,\"points\":3,\"day\":\"2025-01-01\"},\"2025-01-02\"]",
      "expectedJson": "{\"dailyRights\":1,\"bonusRights\":2,\"points\":3,\"day\":\"2025-01-02\"}"
    }
  ]
}