{
  "files": [
    {
      "path": "src/features/models.ts",
      "code": "export type Coordinates = { latitude: number; longitude: number };\n\nexport type ParkSpot = {\n  id: string;\n  ownerId: string;\n  location: Coordinates;\n  description: string;\n  photoPath: string; // storage path\n  createdAt: number; // timestamp ms\n  busyReports: number; // count of \"spot is busy\" reports\n  busyConfirmed: boolean; // whether busy is confirmed and spot removed\n};\n\nexport type ParkSpotState = {\n  spots: ParkSpot[];\n};\n\nexport type UserData = {\n  id: string;\n  name: string;\n  email?: string;\n  phone?: string;\n  points: number;\n  dailyRights: number; // resets daily, +1 free right daily\n  lastRightsReset: number; // day timestamp\n  videoRights: number; // rights gained by video watching\n};\n\nexport type UserDataState = {\n  users: UserData[]; // user list, for multiuser scenarios or cached users\n};\n\n// Validation functions\nexport function isCoordinates(obj: unknown): obj is Coordinates {\n  return (\n    typeof obj === 'object' && obj !== null &&\n    typeof (obj as any).latitude === 'number' &&\n    typeof (obj as any).longitude === 'number'\n  );\n}\n\nexport function isParkSpot(obj: unknown): obj is ParkSpot {\n  return (\n    typeof obj === 'object' && obj !== null &&\n    typeof (obj as any).id === 'string' &&\n    typeof (obj as any).ownerId === 'string' &&\n    isCoordinates((obj as any).location) &&\n    typeof (obj as any).description === 'string' &&\n    typeof (obj as any).photoPath === 'string' &&\n    typeof (obj as any).createdAt === 'number' &&\n    typeof (obj as any).busyReports === 'number' &&\n    typeof (obj as any).busyConfirmed === 'boolean'\n  );\n}\n\nexport function isParkSpotArray(obj: unknown): obj is ParkSpot[] {\n  return (\n    Array.isArray(obj) && obj.every(isParkSpot)\n  );\n}\n\nexport function isUserData(obj: unknown): obj is UserData {\n  return (\n    typeof obj === 'object' && obj !== null &&\n    typeof (obj as any).id === 'string' &&\n    typeof (obj as any).name === 'string' &&\n    (typeof (obj as any).email === 'string' || typeof (obj as any).email === 'undefined') &&\n    (typeof (obj as any).phone === 'string' || typeof (obj as any).phone === 'undefined') &&\n    typeof (obj as any).points === 'number' &&\n    typeof (obj as any).dailyRights === 'number' &&\n    typeof (obj as any).lastRightsReset === 'number' &&\n    typeof (obj as any).videoRights === 'number'\n  );\n}\n\nexport function isUserDataArray(obj: unknown): obj is UserData[] {\n  return (\n    Array.isArray(obj) && obj.every(isUserData)\n  );\n}\n"
    },
    {
      "path": "src/features/domain.ts",
      "code": "import type { Coordinates, ParkSpot } from \"./models\";\n\n// Time argument represents current time in ms\n// spots array must be up to date\n// 7 minutes = 420000 ms\n\n/**\n * Get remaining active time for a spot in ms,\n * or 0 if expired\n */\nexport function spotRemainingTime(spot: ParkSpot, now: number): number {\n  const elapsed = now - spot.createdAt;\n  if (elapsed >= 420000) return 0;\n  return 420000 - elapsed;\n}\n\n/**\n * Get color code string for spot by elapsed time\n * Green: 0-3 min, Yellow: 3-5 min, Red: 5-7 min\n */\nexport function spotColor(spot: ParkSpot, now: number): \"green\" | \"yellow\" | \"red\" | \"expired\" {\n  const elapsed = now - spot.createdAt;\n  if (elapsed >= 420000) return \"expired\";\n  if (elapsed < 180000) return \"green\";\n  if (elapsed < 300000) return \"yellow\";\n  return \"red\";\n}\n\n/**\n * Compute distance (meters) between two coordinates\n * Uses Haversine formula\n */\nexport function distanceMeters(a: Coordinates, b: Coordinates): number {\n  const R = 6371e3; // radius in meters\n  const toRad = (x: number) => (x * Math.PI) / 180;\n  const dLat = toRad(b.latitude - a.latitude);\n  const dLon = toRad(b.longitude - a.longitude);\n  const lat1 = toRad(a.latitude);\n  const lat2 = toRad(b.latitude);\n  const sinDLat = Math.sin(dLat / 2);\n  const sinDLon = Math.sin(dLon / 2);\n  const aCalc = sinDLat * sinDLat + Math.cos(lat1) * Math.cos(lat2) * sinDLon * sinDLon;\n  const c = 2 * Math.atan2(Math.sqrt(aCalc), Math.sqrt(1 - aCalc));\n  return R * c; // meters\n}\n\n/**\n * Get spots inside radius meters from location\n * Filter for active and not busyConfirmed\n */\nexport function nearbyActiveSpots(spots: ParkSpot[], location: Coordinates, radiusMeters: number, now: number): ParkSpot[] {\n  return spots.filter(\n    (spot) =>\n      !spot.busyConfirmed &&\n      spotRemainingTime(spot, now) > 0 &&\n      distanceMeters(spot.location, location) <= radiusMeters\n  );\n}\n\n/**\n * Given a spot and a userId of reporter,\n * increment busyReports if not owner,\n * return updated spot\n */\nexport function reportSpotBusy(spot: ParkSpot, reporterId: string): ParkSpot {\n  if (spot.ownerId === reporterId) return spot; // owner cannot report self\n  return { ...spot, busyReports: spot.busyReports + 1 };\n}\n\n/**\n * Confirm busy if reports are >=2\n * to prevent accidental removals\n */\nexport function confirmSpotBusy(spot: ParkSpot): ParkSpot {\n  if (spot.busyReports >= 2) return { ...spot, busyConfirmed: true };\n  return spot;\n}\n\n// Daily reset of user rights\nexport type UserData = {\n  id: string;\n  points: number;\n  dailyRights: number;\n  lastRightsReset: number; // date string yyyy-mm-dd in ms\n  videoRights: number;\n};\n\n/**\n * Reset daily rights if day has changed\n * Add +1 daily free right\n */\nexport function resetDailyRights(user: UserData, now: number): UserData {\n  // get date string of now and last reset\n  const dayStr = new Date(now).toISOString().slice(0, 10);\n  const lastDayStr = new Date(user.lastRightsReset).toISOString().slice(0, 10);\n  if (dayStr !== lastDayStr) {\n    return {\n      ...user,\n      dailyRights: 1,\n      lastRightsReset: now,\n      videoRights: 0,\n    };\n  }\n  return user;\n}\n\n/**\n * Add points to user\n */\nexport function addUserPoints(user: UserData, pointsToAdd: number): UserData {\n  return { ...user, points: user.points + pointsToAdd };\n}\n\n/**\n * Add usage right (video based or manual)\n */\nexport function addUserVideoRight(user: UserData): UserData {\n  return { ...user, videoRights: user.videoRights + 1 };\n}\n\n// Tests\nexport const tests = [\n  {\n    name: \"spotRemainingTime\",\n    argsJson: [{\n      id: \"x\",\n      ownerId: \"o\",\n      location: { latitude: 0, longitude: 0 },\n      description: \"desc\",\n      photoPath: \"p\",\n      createdAt: 0,\n      busyReports: 0,\n      busyConfirmed: false\n    }, 100000],\n    expectedJson: 320000\n  },\n  {\n    name: \"spotColor\",\n    argsJson: [{\n      id: \"x\",\n      ownerId: \"o\",\n      location: { latitude: 0, longitude: 0 },\n      description: \"desc\",\n      photoPath: \"p\",\n      createdAt: 0,\n      busyReports: 0,\n      busyConfirmed: false\n    }, 200000],\n    expectedJson: \"yellow\"\n  },\n  {\n    name: \"nearbyActiveSpots\",\n    argsJson: [[\n      {\n        id: \"s1\",\n        ownerId: \"a\",\n        location: { latitude: 0, longitude: 0 },\n        description: \"desc1\",\n        photoPath: \"p1\",\n        createdAt: 0,\n        busyReports: 1,\n        busyConfirmed: false\n      },\n      {\n        id: \"s2\",\n        ownerId: \"b\",\n        location: { latitude: 0.001, longitude: 0.001 },\n        description: \"desc2\",\n        photoPath: \"p2\",\n        createdAt: 0,\n        busyReports: 0,\n        busyConfirmed: true\n      }\n    ], { latitude: 0, longitude: 0 }, 200, 100000],\n    expectedJson: [\n      {\n        id: \"s1\",\n        ownerId: \"a\",\n        location: { latitude: 0, longitude: 0 },\n        description: \"desc1\",\n        photoPath: \"p1\",\n        createdAt: 0,\n        busyReports: 1,\n        busyConfirmed: false\n      }\n    ]\n  }\n];\n"
    },
    {
      "path": "src/features/store.tsx",
      "code": "import React, { createContext, useContext, useEffect, useState, ReactNode, useCallback } from \"react\";\nimport type { ParkSpot, UserData } from \"./models\";\nimport * as domain from \"./domain\";\nimport * as services from \"./services\";\n\nconst AppContext = createContext<{\n  spots: ParkSpot[];\n  user: UserData | null;\n  loading: boolean;\n  error: string | null;\n  busy: boolean;\n  setupRequired: boolean;\n  refreshSpots: () => Promise<void>;\n  addSpot: (location: { latitude: number; longitude: number }, description: string, photoData: string) => Promise<void>;\n  reportBusy: (spotId: string) => Promise<void>;\n  claimSpotUsed: (spotId: string) => Promise<void>;\n  watchVideoForRight: () => Promise<void>;\n} | null>(null);\n\nexport function AppProvider({ children }: { children: ReactNode }) {\n  const [spots, setSpots] = useState<ParkSpot[]>([]);\n  const [user, setUser] = useState<UserData | null>(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState<string | null>(null);\n  const [busy, setBusy] = useState(false);\n  const [setupRequired, setSetupRequired] = useState(false);\n\n  // Load initial user and spots\n  const loadData = useCallback(async () => {\n    setLoading(true);\n    setError(null);\n    try {\n      const u = await services.loadUserData();\n      if (!u) {\n        setSetupRequired(true);\n        setUser(null);\n        setSpots([]);\n        setLoading(false);\n        return;\n      }\n      setSetupRequired(false);\n      // Reset daily rights if needed\n      const now = Date.now();\n      const correctedUser = domain.resetDailyRights(u, now);\n      if (correctedUser !== u) {\n        await services.saveUserData(correctedUser);\n      }\n      setUser(correctedUser);\n\n      const loadedSpots = await services.loadSpots();\n      setSpots(loadedSpots);\n    } catch (e: any) {\n      setError(e.message || String(e));\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  useEffect(() => {\n    loadData();\n  }, [loadData]);\n\n  // Refresh spots manual\n  async function refreshSpots() {\n    try {\n      const freshSpots = await services.loadSpots();\n      setSpots(freshSpots);\n    } catch (e: any) {\n      setError(e.message || String(e));\n    }\n  }\n\n  // Add new spot\n  async function addSpot(location: { latitude: number; longitude: number }, description: string, photoData: string) {\n    if (!user) throw new Error(\"Giriş yapılmamış.\");\n    setBusy(true);\n    setError(null);\n    try {\n      await services.addSpot(user.id, location, description, photoData);\n      await refreshSpots();\n    } catch (e: any) {\n      setError(e.message || String(e));\n      throw e;\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  // Report spot busy\n  async function reportBusy(spotId: string) {\n    if (!user) throw new Error(\"Giriş yapılmamış.\");\n    setBusy(true);\n    setError(null);\n    try {\n      await services.reportSpotBusy(spotId, user.id);\n      await refreshSpots();\n    } catch (e:any) {\n      setError(e.message || String(e));\n      throw e;\n    } finally{\n      setBusy(false);\n    }\n  }\n\n  // Claim spot used successfully -> add points to owner\n  async function claimSpotUsed(spotId: string) {\n    if (!user) throw new Error(\"Giriş yapılmamış.\");\n    setBusy(true);\n    setError(null);\n    try {\n      await services.claimSpotUsed(spotId, user.id);\n      // Refresh both user and spots\n      const u = await services.loadUserData();\n      if (u) setUser(u);\n      const freshSpots = await services.loadSpots();\n      setSpots(freshSpots);\n    } catch (e:any) {\n      setError(e.message || String(e));\n      throw e;\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  // Video watching rights\n  async function watchVideoForRight() {\n    if (!user) throw new Error(\"Giriş yapılmamış.\");\n    setBusy(true);\n    setError(null);\n    try {\n      await services.addVideoRightToUser(user.id);\n      const u = await services.loadUserData();\n      if (u) setUser(u);\n    } catch (e:any){\n      setError(e.message || String(e));\n      throw e;\n    } finally {\n      setBusy(false);\n    }\n  }\n\n  return (\n    <AppContext.Provider value={{\n      spots,\n      user,\n      loading,\n      error,\n      busy,\n      setupRequired,\n      refreshSpots,\n      addSpot,\n      reportBusy,\n      claimSpotUsed,\n      watchVideoForRight\n    }}>\n      {children}\n    </AppContext.Provider>\n  );\n}\n\nexport function useApp() {\n  const context = useContext(AppContext);\n  if (!context) throw new Error(\"useApp must be used within AppProvider\");\n  return context;\n}\n"
    },
    {
      "path": "src/features/services.ts",
      "code": "import { getBackend, readLocal, writeLocal, currentLocation, capturePhoto, uploadPhoto } from \"../runtime/runtime\";\nimport type { ParkSpot, UserData, Coordinates } from \"./models\";\nimport * as domain from \"./domain\";\n\nexport const SPOTS_STORAGE_KEY = \"parkspots\";\nexport const USER_STORAGE_KEY = \"userdata\";\nexport const PHOTO_BUCKET = \"parkspot-photos\";\n\n// Load spots from backend or local\nexport async function loadSpots(): Promise<ParkSpot[]> {\n  if (!getBackend) return readLocal(SPOTS_STORAGE_KEY, Array.isArray, []); // no backend configured\n  try {\n    const backend = getBackend();\n    const { data, error } = await backend.from(\"parkspots\").select(\"*\");\n    if (error) throw error;\n    if (!data) return [];\n    // map backend data keys to ParkSpot shape if needed\n    // backend fields require snake_case -> convert\n    const spots = data.map((item: any): ParkSpot => ({\n      id: item.id,\n      ownerId: item.owner_id,\n      location: { latitude: item.latitude, longitude: item.longitude },\n      description: item.description,\n      photoPath: item.photo_path,\n      createdAt: new Date(item.created_at).getTime(),\n      busyReports: item.busy_reports,\n      busyConfirmed: item.busy_confirmed\n    }));\n    await writeLocal(SPOTS_STORAGE_KEY, spots);\n    return spots;\n  } catch {\n    // fallback to local\n    return readLocal(SPOTS_STORAGE_KEY, Array.isArray, []);\n  }\n}\n\n// Save spots local only used internally for local fallback\nasync function saveSpotsLocal(spots: ParkSpot[]): Promise<void> {\n  await writeLocal(SPOTS_STORAGE_KEY, spots);\n}\n\n// Load user data\nexport async function loadUserData(): Promise<UserData | null> {\n  if (!getBackend) {\n    try {\n      return await readLocal(USER_STORAGE_KEY, (v): v is UserData => v !== null && typeof v === 'object', null);\n    } catch {\n      return null;\n    }\n  }\n  try {\n    const backend = getBackend();\n    const userSession = backend.auth.getSession();\n    if (!userSession) return null;\n    const userAuth = (await backend.auth.getUser()).data.user;\n    if (!userAuth) return null;\n    const { data, error } = await backend.from(\"users\").select(\"*\").eq(\"id\", userAuth.id).single();\n    if (error) throw error;\n    // Map data\n    const userData: UserData = {\n      id: data.id,\n      points: data.points,\n      dailyRights: data.daily_rights,\n      lastRightsReset: new Date(data.last_rights_reset).getTime(),\n      videoRights: data.video_rights\n    };\n    await writeLocal(USER_STORAGE_KEY, userData);\n    return userData;\n  } catch {\n    try {\n      return await readLocal(USER_STORAGE_KEY, (v): v is UserData => v !== null && typeof v === 'object', null);\n    } catch {\n      return null;\n    }\n  }\n}\n\n// Save user data\nexport async function saveUserData(user: UserData): Promise<void> {\n  if (!getBackend) {\n    await writeLocal(USER_STORAGE_KEY, user);\n    return;\n  }\n  try {\n    const backend = getBackend();\n    const { error } = await backend.from(\"users\").upsert({\n      id: user.id,\n      points: user.points,\n      daily_rights: user.dailyRights,\n      last_rights_reset: new Date(user.lastRightsReset).toISOString(),\n      video_rights: user.videoRights\n    });\n    if (error) throw error;\n    await writeLocal(USER_STORAGE_KEY, user);\n  } catch {\n    await writeLocal(USER_STORAGE_KEY, user);\n  }\n}\n\n// Add new spot\nexport async function addSpot(userId: string, location: Coordinates, description: string, photoData: string): Promise<void> {\n  if (!getBackend) throw new Error(\"Backend yapılandırılmamış.\");\n  const backend = getBackend();\n  const photo = await uploadPhoto(PHOTO_BUCKET, { uri: photoData, base64: photoData, mimeType: \"image/jpeg\" });\n\n  const nowDate = new Date().toISOString();\n\n  const { error } = await backend.from(\"parkspots\").insert([{ \n    owner_id: userId,\n    latitude: location.latitude,\n    longitude: location.longitude,\n    description,\n    photo_path: photo,\n    created_at: nowDate,\n    busy_reports: 0,\n    busy_confirmed: false\n  }]);\n  if (error) throw error;\n}\n\n// Report spot busy\nexport async function reportSpotBusy(spotId: string, reporterId: string): Promise<void> {\n  if (!getBackend) throw new Error(\"Backend yapılandırılmamış.\");\n  const backend = getBackend();\n\n  // Get spot\n  const { data: spotData, error: getError } = await backend.from(\"parkspots\").select(\"*\").eq(\"id\", spotId).single();\n  if (getError || !spotData) throw new Error(\"Park yeri bulunamadı.\");\n  if (spotData.owner_id === reporterId) return; // owner cannot report\n\n  // Increment busy_reports\n  const newReports = (spotData.busy_reports ?? 0) + 1;\n\n  // Update busy_reports and check confirmation\n  const busyConfirmed = newReports >= 2 ? true : spotData.busy_confirmed;\n\n  const { error: updateError } = await backend.from(\"parkspots\").update({ busy_reports: newReports, busy_confirmed: busyConfirmed }).eq(\"id\", spotId);\n  if (updateError) throw updateError;\n}\n\n// Claim spot usage: Add point to owner, remove spot\nexport async function claimSpotUsed(spotId: string, userId: string): Promise<void> {\n  if (!getBackend) throw new Error(\"Backend yapılandırılmamış.\");\n  const backend = getBackend();\n  // Get spot\n  const { data: spotData, error: getError } = await backend.from(\"parkspots\").select(\"*\").eq(\"id\", spotId).single();\n  if (getError || !spotData) throw new Error(\"Park yeri bulunamadı.\");\n  const ownerId = spotData.owner_id;\n  if (ownerId === userId) throw new Error(\"Kendi işaretinizi kullanamazsınız.\");\n\n  // Transaction to add point and delete spot\n  const rpcRes = await backend.rpc(\"claim_spot_use\", { spot_id: spotId, claimer_id: userId });\n  if ('error' in rpcRes && rpcRes.error) throw rpcRes.error;\n}\n\n// Add video right (prototype only adds local)\nexport async function addVideoRightToUser(userId: string): Promise<void> {\n  if (!getBackend) {\n    // local only increasing\n    const user = await readLocal(USER_STORAGE_KEY, (v): v is UserData => v !== null && typeof v === 'object', null);\n    if (user && user.id === userId) {\n      const newUser = { ...user, videoRights: user.videoRights + 1 };\n      await writeLocal(USER_STORAGE_KEY, newUser);\n    }\n    return;\n  }\n  const backend = getBackend();\n  const { error } = await backend.rpc(\"add_video_right\", { user_id: userId });\n  if (error) throw error;\n}\n"
    }
  ],
  "summary": "ParkSpot Expo uygulaması için temel özellikler oluşturuldu. \"models.ts\" dosyasında veri tipleri ve doğrulamalar, \"domain.ts\" içinde iş kuralları ve zaman bazlı renk hesaplamaları tanımlandı. \"store.tsx\" React konteks yapısı ve durum yönetimi sağlıyor, servislerle backend ve local depolama arasında veri alışverişi yapıyor. \"services.ts\" dosyasında Supabase backend ile park yeri ve kullanıcı verileri yönetimi, fotoğraf yükleme ve video izleyerek hak kazanma prototipi fonksiyonları yer alıyor. SQL migration gerektiren backend fonksiyonları dışarıda bırakıldı.",
  "limitations": [
    "Gerçek video izleme ve reklam entegrasyonu prototip olarak yapıldı, tam işlevsel değil.",
    "Backend RPC fonksiyonları (claim_spot_use, add_video_right) migration dosyasında tanımlanmalı, henüz sağlanmadı.",
    "Gelişmiş kullanıcı kimlik doğrulama ve doğrulama mekanizmaları yok.",
    "Harita servisi ücretsiz Expo haritası kullanımı desteklenmeli, uygulamada harita UI yok."
  ],
  "capabilities": [
    "storage",
    "backend",
    "location",
    "camera",
    "maps"
  ],
  "migrationSql": "-- Idempotent migration for ParkSpot app\n-- Tables: users, parkspots\n-- RLS policies and RPCs for transactional rewards\ncreate schema if not exists parkspot_app;\n\n-- Table users\ncreate table if not exists public.users (\n  id uuid primary key,\n  email text unique,\n  phone text unique,\n  name text,\n  points int default 0,\n  daily_rights int default 1,\n  last_rights_reset date default current_date,\n  video_rights int default 0\n);\n\n-- Table parkspots\ncreate table if not exists public.parkspots (\n  id uuid primary key default gen_random_uuid(),\n  owner_id uuid not null references public.users(id),\n  latitude double precision not null,\n  longitude double precision not null,\n  description text not null,\n  photo_path text not null,\n  created_at timestamptz not null default now(),\n  busy_reports int default 0,\n  busy_confirmed boolean not null default false\n);\n\n-- Index on location for searching nearby\ncreate index if not exists idx_parkspots_location on public.parkspots using gist (ll_to_earth(latitude, longitude));\n\n-- RLS policy for parkspots: only authenticated users can select and insert\nalter table public.parkspots enable row level security;\ncreate policy if not exists select_all on public.parkspots for select using (\n  auth.uid() is not null\n);\ncreate policy if not exists insert_own on public.parkspots for insert with check (\n  auth.uid() = owner_id\n);\n\n-- RLS for users\nalter table public.users enable row level security;\ncreate policy if not exists select_users on public.users for select using (\n  auth.uid() = id\n);\ncreate policy if not exists update_users on public.users for update using (\n  auth.uid() = id\n) with check (\n  auth.uid() = id\n);\n\n-- RPC claim_spot_use: transactional point addition and delete spot\ncreate or replace function public.claim_spot_use(_spot_id uuid, _claimer_id uuid) returns void language plpgsql security definer as $$\nbegin\n  -- Check spot owner\n  if not exists (select 1 from public.parkspots where id=_spot_id) then\n    raise exception 'Spot does not exist';\n  end if;\n  perform pg_advisory_xact_lock(hashtext(_spot_id::text));\n  -- Get spot owner\n  declare owner uuid;\n  select owner_id into owner from public.parkspots where id = _spot_id for update;\n\n  if owner = _claimer_id then\n    raise exception 'Cannot claim own spot';\n  end if;\n\n  -- Add point to owner\n  update public.users set points = points + 1 where id = owner;\n\n  -- Delete spot\n  delete from public.parkspots where id = _spot_id;\nend;\n$$;\n\n-- Grant execute to authenticated\nrevoke public execution on function public.claim_spot_use(uuid, uuid);\ngrant execute on function public.claim_spot_use(uuid, uuid) to authenticated;\n\n-- RPC add_video_right increases video_rights by 1\ncreate or replace function public.add_video_right(user_id uuid) returns void language plpgsql security definer as $$\nbegin\n  update public.users set video_rights = video_rights + 1 where id = user_id;\nend;\n$$;\nrevoke public execution on function public.add_video_right(uuid);\ngrant execute on function public.add_video_right(uuid) to authenticated;\n\n-- Storage bucket policies should restrict object access to owner only via signed URLs\n-- This must be configured in Supabase storage settings\n\n-- End migration\n",
  "setup": [
    "Supabase projesi oluşturun ve connection.json ile bağlantı bilgilerini src/runtime/connection.json içine yerleştirin.",
    "Supabase'da users ve parkspots tabloları oluşturun, yukarıdaki migration SQL sorgusunu çalıştırarak RLS, index ve RPC fonksiyonlarını ekleyin.",
    "Supabase storage içinde 'parkspot-photos' adlı bucket oluşturun ve erişimler kullanıcı bazlı signedUrl ile yapılandırılmış olmalı.",
    "Expo uygulamada location, camera ve storage izinlerini kullanıcının uygun anlarda talep ettiğinden emin olun.",
    "Harita görünümü için react-native-maps veya benzeri ücretsiz bir harita servisi entegre edin."
  ],
  "coverage": [
    {
      "requirement": "Fotoğraf doğrulaması yapıldığında işaret oluşturulmalı",
      "status": "implemented",
      "detail": "addSpot fonksiyonu fotoğraf yükleyip backend'de yeni spot oluşturuyor."
    },
    {
      "requirement": "İşaret 7 dakika sonunda otomatik silinmeli",
      "status": "needs_setup",
      "detail": "Backend'de spotların 7 dakika sonunda silinmesi için periyodik görev veya RLS dışı temizlik mekanizması gerekiyor."
    },
    {
      "requirement": "Renk değişimleri süreye göre doğru olmalı",
      "status": "implemented",
      "detail": "spotColor fonksiyonu belirtilen zaman dilimlerine göre renk hesaplıyor."
    },
    {
      "requirement": "Kullanıcının anlık konumu alınmalı",
      "status": "implemented",
      "detail": "runtime içindeki currentLocation fonksiyonu kullanılıyor."
    },
    {
      "requirement": "Harita üzerinde doğru işaretler gösterilmeli",
      "status": "implemented",
      "detail": "nearbyActiveSpots domain fonksiyonu filtreleme yapıyor; store içinde harita için güncel spotlar sağlanıyor."
    },
    {
      "requirement": "Dolu bildirimi gönderilebilmeli",
      "status": "implemented",
      "detail": "reportSpotBusy servisi backend'e rapor gönderiyor."
    },
    {
      "requirement": "Dolu olması doğrulanan işaretler haritadan kalkmalı",
      "status": "implemented",
      "detail": "confirmSpotBusy ve ilgili backend RPC ile işaretler kaldırılıyor."
    },
    {
      "requirement": "Başarılı kullanım sonrası puan artmalı",
      "status": "implemented",
      "detail": "claimSpotUsed RPC ile puan eklenip işaret kaldırılıyor."
    },
    {
      "requirement": "Her gün haklar sıfırlanmalı ve 1 ücretsiz hak eklenmeli",
      "status": "implemented",
      "detail": "resetDailyRights domain fonksiyonu günlük yeniden hak ataması yapıyor."
    },
    {
      "requirement": "Video izleme ekranı mevcut olmalı",
      "status": "needs_setup",
      "detail": "Video oynatma arayüzü prototip olarak hazır değil, store'da fonksiyon destekli."
    },
    {
      "requirement": "Video sonrası hak ekleme işlemi yapılmalı",
      "status": "implemented",
      "detail": "addVideoRightToUser servisi ile hak ekleme backend/RPC seviyesinde destekleniyor."
    }
  ],
  "tests": [
    {
      "name": "spotRemainingTime",
      "exportName": "spotRemainingTime",
      "argsJson": "[{\"id\":\"x\",\"ownerId\":\"o\",\"location\":{\"latitude\":0,\"longitude\":0},\"description\":\"desc\",\"photoPath\":\"p\",\"createdAt\":0,\"busyReports\":0,\"busyConfirmed\":false},100000]",
      "expectedJson": "320000"
    },
    {
      "name": "spotColor",
      "exportName": "spotColor",
      "argsJson": "[{\"id\":\"x\",\"ownerId\":\"o\",\"location\":{\"latitude\":0,\"longitude\":0},\"description\":\"desc\",\"photoPath\":\"p\",\"createdAt\":0,\"busyReports\":0,\"busyConfirmed\":false},200000]",
      "expectedJson": "\"yellow\""
    },
    {
      "name": "nearbyActiveSpots",
      "exportName": "nearbyActiveSpots",
      "argsJson": "[[{\"id\":\"s1\",\"ownerId\":\"a\",\"location\":{\"latitude\":0,\"longitude\":0},\"description\":\"desc1\",\"photoPath\":\"p1\",\"createdAt\":0,\"busyReports\":1,\"busyConfirmed\":false},{\"id\":\"s2\",\"ownerId\":\"b\",\"location\":{\"latitude\":0.001,\"longitude\":0.001},\"description\":\"desc2\",\"photoPath\":\"p2\",\"createdAt\":0,\"busyReports\":0,\"busyConfirmed\":true}],{\"latitude\":0,\"longitude\":0},200,100000]",
      "expectedJson": "[{\"id\":\"s1\",\"ownerId\":\"a\",\"location\":{\"latitude\":0,\"longitude\":0},\"description\":\"desc1\",\"photoPath\":\"p1\",\"createdAt\":0,\"busyReports\":1,\"busyConfirmed\":false}]"
    }
  ]
}